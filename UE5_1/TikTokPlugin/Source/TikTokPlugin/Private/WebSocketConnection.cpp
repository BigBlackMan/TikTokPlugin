// WebSocketConnection.cpp
#include "WebSocketConnection.h" // Stub class definition

// We intentionally avoid including any Boost/OpenSSL headers here to prevent 'UI' conflicts. 

// Define a log category to keep consistency if someone logs via this TU later. // Future-proofing
DEFINE_LOG_CATEGORY_STATIC(LogTikTokPlugin_Stub, Log, All); // Separate category for stub (unused)
